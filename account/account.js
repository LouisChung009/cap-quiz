const form=document.querySelector("form[data-auth-flow]");const message=document.querySelector("[data-message]");form?.addEventListener("submit",async event=>{event.preventDefault();const flow=form.dataset.authFlow;const submit=form.querySelector("button");submit.disabled=true;message.textContent="正在安全轉送帳號服務…";try{const response=await fetch("/api/auth/start",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({flow,returnTo:"/"})});const result=await response.json();if(response.ok&&result.redirectUrl){location.assign(result.redirectUrl);return}throw new Error(result.message||"帳號服務尚未啟用。")}catch(error){message.textContent=error.message;submit.disabled=false}});
